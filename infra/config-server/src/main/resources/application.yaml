spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          searchLocations:
            - classpath:config/commerce/{application}
#            - classpath:config/telemetry/{application}
server:
  port: 8888