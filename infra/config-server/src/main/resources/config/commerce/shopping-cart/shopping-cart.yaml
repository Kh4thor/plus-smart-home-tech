spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/shopping_cart  # underscore
    username: cart_user
    password: cart_password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

#  sql:
#    init:
#      mode: always
#      schema-locations: classpath:schema.sql

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 0